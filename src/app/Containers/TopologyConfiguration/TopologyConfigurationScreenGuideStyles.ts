import { ApplicationStyles } from "app/Theme";

export default {
    ...ApplicationStyles,
    teste: {
        backgroundColor: 'yellow'
    } as React.CSSProperties,
    listPlaceholder: {
        bottom: 20,
        zIndex: 0,
        width: 400,
        marginTop: 100
    } as React.CSSProperties,
} 