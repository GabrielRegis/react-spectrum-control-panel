import { CriticalEventSnapshot } from './CriticalEventSnapshot';
export class CriticalFragmentationEventSnapshot extends CriticalEventSnapshot {
    gainRate?: number
    fragmentation?: number
    absoluteDifference?: number
}
