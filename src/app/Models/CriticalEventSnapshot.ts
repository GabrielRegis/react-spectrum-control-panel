import { TopologySnapshot } from './TopologySnapshot';
export class CriticalEventSnapshot {
    load?: number
    currentEvent?: number
    eventsAmount?: number
    topologySnapshot?: TopologySnapshot
}