
import { ApplicationStyles } from 'app/Theme';
export default {
    ...ApplicationStyles,
    waveContainer: {
        padding: 0,
        margin: 0,
        width: '100%',
        zIndex: 5,
        overflow: 'hidden'
    } as React.CSSProperties,
};
