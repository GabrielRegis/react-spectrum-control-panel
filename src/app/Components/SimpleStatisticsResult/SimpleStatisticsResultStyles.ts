import { ApplicationStyles } from "app/Theme";

export default {
    ...ApplicationStyles,
    unity: {
        marginLeft: 5,
    } as React.CSSProperties,
    label: {
        opacity: 0.8,
    } as React.CSSProperties,
    result: {
    } as React.CSSProperties
} 